# Copy to .env and fill in. .env is gitignored — never commit it.
# https://openrouter.ai -> Keys
OPENROUTER_API_KEY=
# https://develop.battle.net -> API access -> create client (pregenerate.py only)
BNET_CLIENT_ID=
BNET_CLIENT_SECRET=
